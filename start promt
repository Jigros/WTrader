Add protocol-level disconnect diagnostics to Mineflayer smoke runner.

Observed:
- server ping reports 26.1.2
- client forced to 1.21.11
- CLIENT_CONNECTED succeeds
- server immediately kicks with "Invalid sequence"

Add OBSERVE-ONLY logging for the last ~30 inbound/outbound packet names before kick:
- packet name
- direction
- timestamp
- state (configuration/play)
- do not dump auth tokens or sensitive payloads

On kick print:
- readableReason
- negotiated client version
- server ping version
- last inbound packet names
- last outbound packet names

Keep a bounded ring buffer.
Do not modify packets or connection behavior.

Run tests/typecheck/build.