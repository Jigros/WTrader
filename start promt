Current /slot diagnostics still show no price/seller metadata in Confirm Purchase.

Observed:
Confirm Purchase / generic_9x3
11 red pane
13 purchased item preview
15 lime pane

But /slot 13 only shows:
itemType, displayName, quantity, durability, enchantments

Add a new debug command:

/slotraw <id>

For the selected slot, safely inspect the ORIGINAL Mineflayer/Prismarine Item object before WTrader normalization.

Print bounded/allowlisted fields only:
- name
- type
- count
- metadata
- displayName
- customName
- lore
- nbt
- components
- component keys
- any text/json/chat-component fields
- any rawData/rawNbt fields exposed by the library

Recursively serialize only:
- primitives
- short arrays
- bounded objects
- max depth 6
- max total output size
- redact obviously sensitive unrelated fields

Do not modify packets or trading behavior.

Also add:
/windowraw

that prints bounded raw window metadata and non-empty slot raw metadata keys,
because Donut may encode price/seller outside the normalized Item wrapper.

Run tests/typecheck/build.