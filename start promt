Finish WTrader into a production-ready autonomous DonutSMP AH trader. Audit the current repo first, then implement the remaining core in priority order: real GUI/profile integration, correct listing-slot buying, reliable Donut metadata/price parsing, full buy verification/reconciliation, real sell execution, market history, robust valuation/liquidity/net-profit, fast scanner/hot path, recovery/reconnect, proper DB migrations, multi-bot Redis coordination, realistic backtesting, metrics, Discord control/alerts, and deployment readiness. Reuse existing architecture, remove dead/duplicate code, avoid new heuristics where the provided GUI mechanics already exist, keep PostgreSQL/LLM out of the buy hot path, and run lint/typecheck/tests/build after each major stage. Stop only when the end-to-end flow works: connect → scan → detect opportunity → buy → verify → list → detect sale → realized PnL.
