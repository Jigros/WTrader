# Phase 3 Plan

1. Extend discovery records with semantic button and listing-slot candidates derived only from observed GUI metadata.
2. Add slot-level GUI diffs and guarded semantic action validation; a failed validation must prevent the click.
3. Add a two-stage semantic purchase workflow that validates the listing and confirmation screen independently.
4. Persist learned layouts and transition evidence through the existing discovery boundary.
5. Add deterministic tests using the mock adapter. Real DonutSMP title, lore, and layout mappings remain learned from authorized observations.
